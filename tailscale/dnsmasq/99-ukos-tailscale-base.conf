# Solo la IP Tailscale (evita escuchar en 127.0.0.1 y mezclarse con systemd-resolved).
# listen-address y address=/ukos.tech / address=/dev-ukos.tech los añade apply-ukos-internal-dns-proxy.sh.

bind-interfaces
no-resolv
no-poll
cache-size=256
